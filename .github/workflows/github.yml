name: Publish Website

on:
  push:
    branches:
      - main


jobs:
  web-deploy:
    name: 🚀 Deploy Website Every Commit
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get Latest Code
      uses: actions/checkout@v3
    
    - name: 📂 Sync Files
      uses: SamKirkland/web-deploy@v1
      with:
        target-server: muyi-bilingual.com
        remote-user: dh_5ankzi
        private-ssh-key: ${{ secrets.SSH_KEY }}
        destination-path: /home/dh_5ankzi/muyi-bilingual.com/wp-content/themes/church-site-jacob-terri/